<h1>All Contacts</h1>

<ul>
<% @@rolodex.contacts.each do |contact| %>
  <li><%= contact.first_name %> 
      <%= contact.last_name %>
      <%= contact.email %>
      <%= contact.note %>
      <a href="/contacts/<%= contact.id %>">View Profile</a>
  </li>
<% end %>
</ul>
